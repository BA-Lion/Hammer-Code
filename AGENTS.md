# Coding Agent 工作约定

本仓库使用 `.ai/` 作为跨会话的项目上下文、设计约束与验证证据库。`.ai/` 是辅助工作系统，不替代当前用户指令、运行环境中更高优先级的指令、实际代码、配置、测试或正式产品文档。

## 会话启动

开始处理仓库任务时：

1. 读取 [`.ai/index.md`](.ai/index.md)，确认当前任务、有效架构约束、可执行 implementation、未解决问题和近期验证证据。
2. 读取 [`.ai/project.md`](.ai/project.md)，了解项目技术栈、仓库地图、标准命令、修改限制和权威资料入口。
3. 只继续读取索引中与当前任务相关的记录；不要无目的地加载全部 `.ai/`、模板或归档。
4. 首次维护 `.ai/`、判断记录职责或工作流存在疑问时，读取 [`.ai/readme.md`](.ai/readme.md)。

如果 `.ai/project.md` 仍为 `uninitialized` 或包含 `TBD`，必须把相应内容视为未知：优先从仓库事实中核实；无法可靠确定且会影响实现时，再向用户确认。不得把占位符或模板示例当成项目事实。

## 权威边界

- 当前用户指令和当前生效的 Agent 指令定义本次工作的授权与行为边界。
- `architecture/` 记录长期设计意图和架构不变量；只有 `accepted` 状态构成当前约束。
- `decision/` 记录任务级方案与取舍；只有 `approved` 状态可作为 active implementation 的上游。
- `implementation/` 记录执行契约；只有 `approved` 或 `in_progress` 状态可作为编码依据。
- 代码、配置和可重复测试反映当前实际状态；`test/result/` 只是特定时间点的证据。
- `.ai/` 中的历史批准不能扩大当前授权，也不能替代当前仍然需要的人类批准。

发现文档、代码、测试或用户需求彼此冲突时，不得静默选择一方或自行扩大范围。先判断是实现缺陷、文档过期还是需求变化；在继续会造成实质性分歧时，停止相关改动并向用户说明冲突与可选处理方式。

## 风险分级与记录要求

开始修改前，按 [`.ai/readme.md`](.ai/readme.md) 判断任务风险：

- **L0（轻量修改）**：文案、注释、局部样式和显而易见的低风险小修复。无需强制创建 `.ai` 记录。
- **L1（标准任务）**：跨文件修改、普通功能、局部重构或需要恢复上下文的缺陷。创建 `task/current/` 记录；存在明显方案取舍时创建 decision。
- **L2（复杂或模型交接）**：复杂功能、跨模块修改或需要交给另一执行模型。必须创建 task、decision、implementation 和验证结果摘要；implementation 获得批准后方可执行。
- **L3（高风险或架构变更）**：涉及架构边界、数据迁移、安全、权限、破坏性兼容或不可逆操作。必须创建或更新 architecture，并创建 task、decision、implementation、验证记录以及回滚、迁移或恢复策略；取得与风险相称的明确批准后方可执行。

所有 `.ai` 记录应从对应模板创建，使用规定的 front matter、状态值和仓库相对链接。新增、批准、阻塞、完成、替代或归档记录时，同步维护 `.ai/index.md`。

## 执行规则

- 修改前检查实际代码和工作树；保留用户已有改动，不覆盖或清理任务范围外的内容。
- 遵循 `architecture → decision → implementation → test/result` 的约束链；下游记录不得突破上游记录。
- implementation 与代码现状或上游记录冲突、目标无法无歧义定位、需要修改未授权文件或扩大范围时，停止并上报。
- 新增生产依赖、执行数据迁移、破坏兼容性或采取不可逆操作前，必须获得当前明确授权。
- 不将模糊描述自行扩展成新的产品决定或架构决定。
- 改动应保持在满足目标所需的最小合理范围内，并与仓库现有风格一致。

## 验证与收尾

- 执行与风险相称的格式化、静态检查、测试和构建；优先使用 `.ai/project.md` 登记的标准命令。
- 不得只报告“测试通过”。说明实际执行的命令、环境、结果以及未执行的检查和原因。
- L2/L3 任务将脱敏后的验证摘要写入 `test/result/`；不得保存密钥、令牌、个人信息、客户原始数据或未脱敏日志。
- 任务完成时更新相关 task、implementation 和 `.ai/index.md`；未解决事项进入 `problem/` 或 `task/future/`，稳定知识按职责沉淀到 `doc/`、`attention/` 或 `test/experience/`。
- 修改 `.ai/` 结构或记录后运行：

  ```powershell
  python .ai/test/script/validate_harness.py
  ```

- 最终回复应简明列出完成内容、关键文件、验证结果、残余风险或阻塞。
